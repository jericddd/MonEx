# Secrets / credentials - never commit these
saved OAth/
*secret*.txt
*Secret*.txt
*.env
.env

# Local dev / tooling (root only — x-bot/package.json is required for Railway deploy)
/node_modules/
/package.json
/package-lock.json
