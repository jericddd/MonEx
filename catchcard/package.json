{
  "name": "catchcard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CatchCard — mint NFT cards via @CatchCard on X (Monad testnet)",
  "scripts": {
    "test": "npm run test:rules && npm run test:contracts",
    "test:rules": "node --test shared/rules.test.js",
    "test:contracts": "cd contracts && forge test",
    "dev:bot": "npm run dev --prefix bot"
  },
  "engines": {
    "node": ">=20"
  }
}
